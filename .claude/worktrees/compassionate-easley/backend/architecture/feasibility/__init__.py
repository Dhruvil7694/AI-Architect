"""
architecture.feasibility
------------------------

Aggregation and derived-metric helpers for feasibility reporting.

This package does not run heavy geometry pipelines itself; it consumes
outputs from existing engines (envelope, placement, core, skeleton,
rules) and computes lightweight, deterministic metrics for reporting.
"""

